# Homework 3, part 1 — Fan out

*Set after day 3. This is the weekend, so there are two parts.*

Read `FACTORY.md`, then the feature files in `features/`. Together they
are the whole spec of the factory at this point, not just the new parts.

The doer's work now fans out to three reviewers at once — same job, three
different providers' models — and a synthesiser reads all three reports
and decides. Wire it however you like.

A note on words: the three machines are **reviewers**. They assess and
report; they do not decide. The synthesiser decides. Fan out, reviewers,
synthesiser — that whole assembly is the validation step.

Do part 2 after this one.
